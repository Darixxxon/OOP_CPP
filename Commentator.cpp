/*#include "Commentator.h"

Commentator::Commentator(){
}

void Commentator::comment()
{
	for (string comment : commentator_) {
		cout << comment << endl;
	}
}

void Commentator::addKillComment(Organism* first, Organism* second) {
	commentator_.push_back(first->name_ + " killed " + second->name_);
}

void Commentator::addEatComment(Organism* first, Organism* second) {
	commentator_.push_back(first->name_ + " killed " + second->name_);
}

void Commentator::clearComm() {
	commentator_.clear();
}*/